#!/usr/bin/env python3
import random

libraries = [
    "yt-dlp",
    "httpx",
    "pyinfra",
    "opencv+mediapipe",
    "socket+scapy",
    "shodan",
    "paramiko",
    "playwright",
    "curl_cffi",
    "duckdb"
]

print(f"the library you must learn and master, {random.choice(libraries)}")
